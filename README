1_ install the "emoji-picker-react" & "react-icons" packages


2_ in your tailwind.config file in the Plugins add
        function ({ addVariant }) {
            addVariant('ch', '& > *');
            addVariant('ch-hover', '& > *:hover')
        }
    as an object

3_wrap this chat support component to the root layout of your project